name: Deploy

# PROMOTION MODEL — what deploys where, and why.
#
#   push to `Preview`            →  PREVIEW        (preview.seqtek.com + the
#                                                     temporary ww3.seqtek.com
#                                                     PROD-preview lane —
#                                                     account [REDACTED_AWS_ACCOUNT_ID])
#   merge to `main`              →  STAGING / UAT  (seqtek-preview.com —
#                                                     account 600881993295)
#   publish a `vX.Y.Z` release   →  PRODUCTION     (CloudFront URL until cutover)
#
# `Preview` (account [REDACTED_AWS_ACCOUNT_ID]) and `main`'s STAGING (account 600881993295)
# are two SEPARATE environments in two SEPARATE AWS accounts, each with its own
# `infra/cdk.json` block, ECR repo, and OIDC deploy role — this is not a
# rename, it's a parallel track added 2026-08-11/12 while the Preview branch
# was stood up by hand (see docs/INFRASTRUCTURE_RUNBOOK.md). PRODUCTION here
# is still the ORIGINAL release-gated env (`envName: prod` in cdk.json) — it
# has never had infrastructure deployed and is a separate decision from
# either preview lane above.
#
# Merging to main is NOT a production deploy. main is the staging/UAT
# environment: every merged PR lands there immediately so it can be reviewed
# against real infrastructure. Production moves only when a Release-Please
# release is published — i.e. when someone merges the release PR, which cuts
# the `vX.Y.Z` tag and the GitHub Release this workflow listens for. That is
# the deliberate human gate on going live, and it is why cutting a release is
# treated as an outward-facing action.
#
# Release-Please tags are `vX.Y.Z` (`include-v-in-tag: true`,
# `include-component-in-tag: false` in release-please-config.json).
# `release-please.yml` dispatches this workflow after cutting one — see the
# `release:` trigger below for why the release event alone is not enough.
#
# The deployed image is IMMUTABLE: `-c imageTag` stamps the task definition
# with `vX.Y.Z` on a release and the commit SHA otherwise, so a task replaced
# later re-pulls that exact build and rollback is a redeploy of an older tag.
#
# `workflow_dispatch` exists for the cases a trigger can't cover: the FIRST
# deploy into a fresh account (no release to replay), a stack-scoped redeploy,
# or re-running a deploy without cutting a version.
#
# Account resolution: `vars.AWS_ACCOUNT_ID` is read from the GitHub *Environment*
# (Settings → Environments → preview / staging / production), falling back to
# the repo-level variable — which is why staging keeps working untouched (its
# account is the repo-level default; preview and production each set their own
# environment-scoped override). The three environments MAY live in different
# AWS accounts; the ECR repository and the account-wide OIDC provider each have
# an explicit owner in `infra/cdk.json` (`ownsAccountEcrRepository` /
# `ownsAccountOidcProvider`), set `true` on every env that owns one in its own
# account. See ARCHITECTURE.md § Environments & isolation.

on:
  push:
    branches:
      - main
      - Preview
    # Skip the redeploy for changes that can't affect the running image.
    # Release-Please's PR-merges and docs-only PRs both fall into this
    # bucket. `paths-ignore` skips the workflow entirely if EVERY changed
    # path matches — mixed code+docs commits still trigger.
    paths-ignore:
      - CHANGELOG.md
      - .release-please-manifest.json
      - docs/**
      - specs/**
      - README.md
      - CLAUDE.md
      - LICENSE
  # Production, for releases published by a HUMAN from the UI.
  #
  # This trigger does NOT catch Release-Please's own releases: that action runs
  # as the default GITHUB_TOKEN, and GitHub does not create workflow runs from
  # events raised by GITHUB_TOKEN. Relying on it alone leaves the production
  # path inert with nothing to show for it — no failed run, no notification.
  # `release-please.yml` therefore dispatches this workflow explicitly after
  # cutting a release (workflow_dispatch is one of the two documented exceptions
  # to the GITHUB_TOKEN rule). Both routes land on the same job.
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      env:
        description: 'Target environment'
        default: staging
        type: choice
        options:
          - staging
          - preview
          - prod
      stack-filter:
        description: >-
          CDK stack glob, WITHOUT the Seqtek<Env> prefix (it is prepended from
          `env`). Default `*Compute` redeploys just the app tier. Use `*` for a
          full-environment redeploy. The FIRST deploy into a new account is done
          locally instead (runbook §1.5) — this workflow cannot bootstrap it,
          since the deploy role it assumes is created by the Network stack.
        default: '*Compute'
        type: string

permissions:
  id-token: write
  contents: read
  deployments: write # so the GitHub Environment records the deployment

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  AWS_REGION: us-east-1
  ECR_REPOSITORY: seqtek-website

# Pairs with CloudFormation's native stack-update lock — a second run started
# while one is in flight queues rather than racing. Never cancel a deploy
# mid-flight (FR-016). Keyed per env so a prod deploy never queues behind a
# staging or preview one, and preview never queues behind staging.
#
# The "is this a preview push/dispatch" test is repeated verbatim across
# `concurrency.group`, `environment:`, and the `env:` block below — job-level
# `env:` is not in scope for `environment:`/`concurrency:` (they're evaluated
# before it exists), so there's no single place to define it once.
concurrency:
  group:
    deploy-${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') && 'prod' ||
    ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') || github.event.inputs.env
    == 'preview') && 'preview' || 'staging' }}
  cancel-in-progress: false

jobs:
  deploy:
    name: Build, push, deploy
    runs-on: ubuntu-latest
    # `production` is the conventional GitHub Environment name and is where a
    # required-reviewer gate would hang; staging stays `staging`; `preview` is
    # its own environment too (account [REDACTED_AWS_ACCOUNT_ID] — see the promotion-model
    # comment at the top of this file), just with no protection rules.
    environment:
      ${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') && 'production' ||
      ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') || github.event.inputs.env
      == 'preview') && 'preview' || 'staging' }}
    outputs:
      site-url: ${{ steps.site.outputs.url }}
    env:
      # A release publish is ALWAYS prod; a push to Preview (or a manual
      # dispatch with env=preview) is ALWAYS preview; everything else honours
      # the dispatch input and defaults to staging. STACK_PREFIX must match
      # `stackName()` in infra/lib/construct-utils.ts: 'staging' →
      # SeqtekStaging*, 'preview' → SeqtekPreview*, 'prod' → SeqtekProd*.
      ENV_NAME:
        ${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') && 'prod' ||
        ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') || github.event.inputs.env
        == 'preview') && 'preview' || 'staging' }}
      STACK_PREFIX:
        ${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') && 'SeqtekProd' ||
        ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') || github.event.inputs.env
        == 'preview') && 'SeqtekPreview' || 'SeqtekStaging' }}
      ROLE_NAME: ${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') &&
        'SeqtekProdDeploy' ||
        ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') || github.event.inputs.env
        == 'preview') && 'SeqtekPreviewDeploy' || 'SeqtekStagingDeploy' }}
      STACK_FILTER:
        ${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') && 'SeqtekProd' ||
        ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') || github.event.inputs.env
        == 'preview') && 'SeqtekPreview' || 'SeqtekStaging'
        }}${{ github.event.inputs.stack-filter || '*Compute' }}
    steps:
      # FAIL CLOSED on an unconfigured production environment.
      #
      # GitHub auto-creates an environment that a workflow references but that
      # does not exist — with no protection rules and no secrets. Since the prod
      # OIDC trust now pins `environment:production` rather than a git ref, that
      # auto-created environment would satisfy the trust and deploy completely
      # ungated. The old ref pin at least failed closed; this restores that
      # property from inside the repo, where it can't be forgotten.
      #
      # Set `PROD_ENVIRONMENT_CONFIGURED=true` as a variable ON the production
      # environment once its deployment branch/tag policies (and reviewers, if
      # any) are in place — docs/INFRASTRUCTURE_RUNBOOK.md §1.2.
      - name: Refuse an unconfigured production environment
        if: ${{ env.ENV_NAME == 'prod' }}
        run: |
          if [ "${{ vars.PROD_ENVIRONMENT_CONFIGURED }}" != "true" ]; then
            echo "::error::The 'production' GitHub Environment is not configured."
            echo "::error::Set its deployment branch/tag policies, then set the"
            echo "::error::variable PROD_ENVIRONMENT_CONFIGURED=true on that"
            echo "::error::environment. See docs/INFRASTRUCTURE_RUNBOOK.md §1.2."
            exit 1
          fi
          echo "production environment reports configured."

      # On a release, build the TAGGED commit, not whatever main has drifted
      # to since the release PR merged.
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'release' && github.event.release.tag_name || github.ref }}

      - name: Configure AWS credentials (${{ env.ENV_NAME }} OIDC role)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/${{ env.ROLE_NAME }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-deploy-${{ env.ENV_NAME }}-${{ github.run_id }}

      - name: Resolve ECR registry + image tags
        id: img
        run: |
          set -euo pipefail
          REGISTRY="${{ vars.AWS_ACCOUNT_ID }}.dkr.ecr.${AWS_REGION}.amazonaws.com"
          SHA_TAG="${GITHUB_SHA::7}"
          IMAGE="${REGISTRY}/${ECR_REPOSITORY}"
          # Version tag, derived from the REF rather than the event name.
          #
          # Keying this on `github.event_name == 'release'` looked right but was
          # dead code on the path that actually deploys production: release-please
          # runs as GITHUB_TOKEN, so its release never raises a `release` event —
          # that is the whole reason release-please.yml dispatches this workflow
          # instead. On that dispatch the event is `workflow_dispatch`, so the
          # version tag was never applied and prod fell back to a bare SHA.
          #
          # The ref is `refs/tags/vX.Y.Z` on BOTH paths (the dispatch passes
          # `--ref "$TAG"`, a human-published release checks out the tag), so it
          # is the reliable source.
          VERSION_TAG=""
          case "$GITHUB_REF" in
            refs/tags/v*) VERSION_TAG="${GITHUB_REF#refs/tags/}" ;;
          esac
          {
            echo "registry=${REGISTRY}"
            echo "image=${IMAGE}"
            echo "sha-tag=${SHA_TAG}"
            echo "version-tag=${VERSION_TAG}"
          } >> "$GITHUB_OUTPUT"

      - name: ECR login
        run: |
          aws ecr get-login-password --region "$AWS_REGION" \
            | docker login --username AWS --password-stdin "${{ steps.img.outputs.registry }}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build + push image
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: true
          provenance: false
          # Immutable tags only. When both environments share one account they
          # share the `seqtek-website` repository, so a bare moving `:latest`
          # rewritten by every staging merge would be picked up by production on
          # its next instance replacement. The SHA (or `vX.Y.Z`) tag is what
          # makes "merging to main does not touch production" true at the image
          # layer and not only at the trigger layer. `:latest-<env>` remains as
          # a convenience pointer; nothing deploys from it.
          tags: |
            ${{ steps.img.outputs.image }}:${{ steps.img.outputs.sha-tag }}
            ${{ steps.img.outputs.image }}:latest-${{ env.ENV_NAME }}
            ${{ steps.img.outputs.version-tag != '' && format('{0}:{1}', steps.img.outputs.image, steps.img.outputs.version-tag) || '' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # NEXT_PUBLIC_* client IDs are build-time-inlined into the browser
          # bundle, so runtime SSM provisioning can't deliver them — they ride
          # as build args. All values are public by definition (shipped to
          # every visitor's browser; portal ID + workshop GUID are committed
          # in INTEGRATIONS.md §1.2). CI's own builds pass no args, keeping
          # test runs hermetic (forms stay in stub mode there by design).
          #
          # NEXT_PUBLIC_HUBSPOT_CONTACT_FORM_ID set 2026-06-22: Chad created the
          # Contact form; the GUID was verified by a test submit (all fields
          # accepted, CAPTCHA off — INTEGRATIONS.md §1.2). Live on next deploy.
          #
          # Deliberately EMPTY until their gates clear:
          #  - NEXT_PUBLIC_GTM_ID: GTM-54KBJ2Z3 is the LIVE Wix-era container;
          #    loading it on staging before the spec 008 container rebuild
          #    would fire its legacy tags uncontrolled. Set it as part of the
          #    spec 008 GTM-UI/staging tail.
          build-args: |
            NEXT_PUBLIC_HUBSPOT_PORTAL_ID=8504846
            NEXT_PUBLIC_HUBSPOT_WORKSHOP_FORM_ID=66dba2bf-f099-44d5-8c6e-f24292cefe53
            NEXT_PUBLIC_HUBSPOT_CONTACT_FORM_ID=8dc61ff4-9f95-46c5-b43e-8c82a394de42
            NEXT_PUBLIC_GTM_ID=

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: |
            package-lock.json
            infra/package-lock.json

      - name: Install infra dependencies
        run: npm --prefix infra ci

      - name: cdk deploy (${{ env.STACK_FILTER }})
        # `-c imageTag` stamps the LaunchTemplate with the IMMUTABLE tag just
        # pushed — `vX.Y.Z` on a release, the commit SHA otherwise. An instance
        # replaced later re-pulls that exact build rather than whatever a moving
        # tag has come to point at, so a running production instance is
        # traceable to a CHANGELOG entry and rollback is a redeploy of an older
        # tag. It also means a staging merge has no moving tag to overwrite.
        env:
          IMAGE_TAG: ${{ steps.img.outputs.version-tag != '' && steps.img.outputs.version-tag ||
            steps.img.outputs.sha-tag }}
        run: |
          echo "Deploying image tag: $IMAGE_TAG"
          npm --prefix infra run cdk -- deploy -c env="$ENV_NAME" -c imageTag="$IMAGE_TAG" "$STACK_FILTER" \
            --require-approval never \
            --outputs-file cdk-outputs.json

      # NO explicit instance refresh.
      #
      # It used to be required: with a moving `:latest` tag the UserData was
      # byte-identical between deploys, so CDK produced no LaunchTemplate change
      # and the ASG never rolled — the new image had to be forced in.
      #
      # With an immutable `-c imageTag`, the UserData now differs on every new
      # build, so the LaunchTemplate gets a new version, and the ASG references
      # it by `LatestVersionNumber` — CloudFormation performs the rolling update
      # itself (MaxBatchSize 1, MinInstancesInService = asgMinCapacity) and
      # `cdk deploy` blocks until it finishes and the instances are healthy.
      # Triggering a refresh on top of that would replace every instance a
      # SECOND time on every deploy. Re-deploying an unchanged tag is correctly
      # a no-op: same image, nothing to roll.

      - name: Resolve site URL (from ${{ env.STACK_PREFIX }}Edge output)
        id: site
        run: |
          set -euo pipefail
          URL=$(aws cloudformation describe-stacks \
            --stack-name "${STACK_PREFIX}Edge" \
            --query "Stacks[0].Outputs[?OutputKey=='SiteUrl'].OutputValue | [0]" \
            --output text)
          if [ -z "$URL" ] || [ "$URL" = "None" ]; then
            echo "Could not resolve ${STACK_PREFIX}Edge SiteUrl output."
            exit 1
          fi
          echo "url=${URL}" >> "$GITHUB_OUTPUT"

  smoke:
    name: Post-deploy smoke
    needs: deploy
    runs-on: ubuntu-latest
    # Runs inside the official Playwright image — browsers + system libs
    # preinstalled. The `npx playwright install --with-deps chromium` apt-get
    # hang on bare GH-hosted runners is the reason this job is split out.
    # Bump the tag in lockstep with @playwright/test in package.json.
    container:
      image: mcr.microsoft.com/playwright:v1.58.2-jammy
      options: --ipc=host
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install root dependencies
        run: npm ci

      - name: Post-deploy smoke
        env:
          # POST_DEPLOY_URL drives the spec under test.
          # PLAYWRIGHT_BASE_URL is what playwright.config.ts checks to skip
          # spinning up the local `npm run dev` webServer — that webServer
          # path needs PAYLOAD_SECRET / DATABASE_URL and is only meant for
          # the PR-time tests job. Setting PLAYWRIGHT_BASE_URL switches us
          # into the external-server branch of the config.
          POST_DEPLOY_URL: ${{ needs.deploy.outputs.site-url }}
          PLAYWRIGHT_BASE_URL: ${{ needs.deploy.outputs.site-url }}
        run: npx playwright test tests/e2e/post-deploy-smoke.e2e.spec.ts

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: post-deploy-playwright-report
          path: playwright-report/
          retention-days: 7

  notify:
    name: Notify Slack
    needs: [deploy, smoke]
    # Report every deploy outcome (success and failure), but stay quiet if the
    # run was cancelled. A failed/skipped `deploy` makes `smoke` skip, so
    # success requires BOTH jobs green. `site-url` (e.g. https://preview.seqtek.com/)
    # flows straight through from the `deploy` job's `Resolve site URL` step —
    # slack-notify.yml renders it as a clickable link in the message.
    if: ${{ !cancelled() }}
    uses: ./.github/workflows/slack-notify.yml
    with:
      workflow-name:
        Deploy (${{ (github.event_name == 'release' || github.event.inputs.env == 'prod') &&
        'production' ||
        ((github.event_name == 'push' && github.ref == 'refs/heads/Preview') ||
        github.event.inputs.env == 'preview') && 'preview' || 'staging' }})
      status: ${{ (needs.deploy.result == 'success' && needs.smoke.result == 'success') && 'success' || 'failure' }}
      site-url: ${{ needs.deploy.outputs.site-url }}
    secrets: inherit
