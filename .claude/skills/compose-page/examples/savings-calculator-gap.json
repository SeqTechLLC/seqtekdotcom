{
  "kind": "gap",
  "brief": "A localshoring landing page whose centerpiece is an interactive savings calculator: the visitor enters team size and current offshore rate, drags a slider, and sees projected annual savings recompute live on the page. Target collection: pages.",
  "collection": "pages",
  "missingBlock": {
    "name": "savings-calculator",
    "reason": "The brief's centerpiece is a stateful, client-side widget: inputs and a slider that recompute a result as the visitor types. No block renders editable inputs or live computation. `items` shows fixed, authored figures, and `embed` could host a third-party calculator, but the brief asks for a first-party one. This goes to Kenn as a gap; it is not built without his sign-off (ADR 0013).",
    "nearestExisting": "items"
  }
}
