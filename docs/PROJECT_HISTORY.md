# SEQTEK Website — Project History

Archive of completed roadmap items, moved out of `ROADMAP.md` once finished. Roadmap stays a short list of what's open; history stays here for traceability. IDs preserved so old commits and cross-references still resolve.

Entries are listed roughly in completion order within each section.

---

## Research (R)

| ID  | Task                                                                                     | Output                                                               | Closed  |
| --- | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------- |
| R-1 | Hinge Research Institute studies on professional services branding and "Visible Experts" | Synthesized in `BRAND_STRATEGY_RESEARCH.md` §2                       | 2026-05 |
| R-2 | Origin-story B2B consulting case studies (5-10 firms with strong narratives)             | Ten-firm table in `BRAND_STRATEGY_RESEARCH.md` §3                    | 2026-05 |
| R-3 | Oklahoma businesses honoring Native heritage in their branding — patterns and tone       | `BRAND_STRATEGY_RESEARCH.md` §4 (with flagged thin-finding caveats)  | 2026-05 |
| R-4 | B2B trust signal research for professional services sites                                | `BRAND_STRATEGY_RESEARCH.md` §5 — consolidated citations             | 2026-05 |
| R-5 | Edelman Trust Barometer 2024+2025 — B2B sections                                         | Cited within `BRAND_STRATEGY_RESEARCH.md` §5                         | 2026-05 |
| R-6 | Competitor brand audit — 5-8 comparable regional firms                                   | Ten-firm audit + positioning gaps in `BRAND_STRATEGY_RESEARCH.md` §6 | 2026-05 |

---

## Design & engineering (D)

| ID   | Task                                                                                      | Output                                                                                                                                                                                                                                                                                                                                                                                                     | Closed  |
| ---- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| D-1  | Design system extension — type scale, color ramps, spacing, radius, shadow, motion tokens | `docs/DESIGN_SYSTEM.md` — major-third type scale, ramps from brand seeds, AA floor / AAA hero contrast, logo usage rules. Token reference written in v4 `@theme` syntax; v3 `tailwind.config.mjs` translation is a Phase 1 task.                                                                                                                                                                           | 2026-05 |
| D-2  | Component / block inventory                                                               | `docs/BLOCK_LIBRARY.md`                                                                                                                                                                                                                                                                                                                                                                                    | 2026-05 |
| D-4  | ARCHITECTURE.md updates: Testing Strategy + CDK Infrastructure sections                   | §12 (Testing) + §13 (Infrastructure as Code) added                                                                                                                                                                                                                                                                                                                                                         | 2026-05 |
| D-5  | Email/SMTP for Payload (auth + password reset) — SES integration spec                     | New INTEGRATIONS.md §6 Transactional Email (AWS SES) — SDK approach, bounce alarm at 5%, env vars added. **Scope note:** Password-reset use becomes moot once D-14 (Google SSO) ships, but SES is still required for any future transactional email.                                                                                                                                                       | 2026-05 |
| D-6  | CSP rollout mechanism — report endpoint + promote-to-enforce trigger                      | INTEGRATIONS.md §8 Rollout mechanism subsection — `/api/csp-report`, CloudWatch metric filter, 5-item promote checklist                                                                                                                                                                                                                                                                                    | 2026-05 |
| D-7  | S3 → CloudFront origin auth — private bucket + Origin Access Control                      | New "Media Storage — S3 + CloudFront with OAC" subsection added to ARCHITECTURE.md §5                                                                                                                                                                                                                                                                                                                      | 2026-05 |
| D-8  | Migration script field-mapping spec (audit JSON → Payload collections)                    | `docs/CONTENT_MIGRATION.md` — per-collection field mapping, plain-text segmentation strategy, idempotency design                                                                                                                                                                                                                                                                                           | 2026-05 |
| D-9  | Auth/roles workflow — draft/publish/scheduled-publish permissions                         | Permissions matrix + scheduled-publish design added to ARCHITECTURE.md §6                                                                                                                                                                                                                                                                                                                                  | 2026-05 |
| D-10 | GTM consent bridge — implement `__hs_opt_in_consent` trigger                              | INTEGRATIONS.md §2.2 Implementation subsection — bridge `<script>` snippet, `infra/gtm/container.json` versioning, consent reqs on pixel tags                                                                                                                                                                                                                                                              | 2026-05 |
| D-11 | Form submission failure UX (retry/queue/error states)                                     | INTEGRATIONS.md §1.2 Failure handling — state machine, error-class table, dataLayer events, mailto fallback                                                                                                                                                                                                                                                                                                | 2026-05 |
| D-12 | Error pages — 404 + 500 + maintenance mode designs                                        | `docs/ERROR_PAGES.md` — 404, 500, maintenance, slow-request handling with tracking + a11y                                                                                                                                                                                                                                                                                                                  | 2026-05 |
| D-13 | Stack spike: scaffold exact versions and verify build (Phase 1 Task 1.0)                  | Stack validated on `spike/stack-validation`: Next 16.2.3 + React 19.2.4 + Payload 3.84 + Postgres 16 + Tailwind v3.4 + Lexical. Playwright smoke passes against both dev (`:3100`) and a Docker container (`:3200`, 327MB image). Spike-only: `force-dynamic` on the homepage so `docker build` doesn't need DB/secrets — Phase 1 switches to ISR. Pinned versions are now load-bearing in `package.json`. | 2026-05 |

---

## Doc fixes (F)

| ID  | Fix                                                                                                                             | Notes                                                               | Closed  |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------- |
| F-1 | INTEGRATIONS.md §11 (was §10) — remove "Amplify Console" reference; replace with CloudFront + ACM                               | —                                                                   | 2026-05 |
| F-2 | CONTENT-REQUIREMENTS.md — mark Mission/Vision/Values as resolved (in brand kit), note core values still need behavioral rewrite | —                                                                   | 2026-05 |
| F-3 | ARCHITECTURE.md — pin Next/React/Payload/Tailwind versions after D-13 spike                                                     | Key Packages table now shows concrete pins from `package.json`      | 2026-05 |
| F-4 | CSP `frame-src` allowlist — add `meetings.hubspot.com` and `*.hubspotusercontent.com`                                           | Synced across INTEGRATIONS.md §8 (was §7) and ARCHITECTURE.md §6    | 2026-05 |
| F-5 | CONTENT-REQUIREMENTS.md §1.E — bump WCAG citation to 2.2 AA throughout (mixed 2.1/2.2 today)                                    | Audit found the doc was already consistent at 2.2 — no edits needed | 2026-05 |

---

## Branding & narrative (BR)

| ID   | Decision                                     | Output                                                                                                                                                                                                                                                                                                                                                                | Closed     |
| ---- | -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| BR-1 | Sequoyah heritage in brand narrative         | Lean-in / layered approach accepted. Decision and Touchstone connection captured in `docs/decisions/0003-sequoyah-brand-narrative.md`; full research case in `BRAND_STRATEGY_RESEARCH.md` §1, §3, §4, §7. SEQTEK stays the surface mark; homepage carries one depth sentence; `/about/our-story` carries the full narrative + cultural-acknowledgement line.          | 2026-05-20 |
| BR-2 | Body font — Avenir (paid) vs free substitute | **Nunito Sans** selected as the open-source Avenir analogue (humanist warmth, similar terminals, SIL OFL licensed, self-hostable). Applied to `docs/DESIGN_SYSTEM.md` §3 and §14 token reference. Avenir Book retained for print materials per the brand kit.                                                                                                         | 2026-05-20 |
| BR-3 | Canonical physical address                   | **12 N Cheyenne Ave., Tulsa, OK 74103** (phone 918-493-7200). Current footer / contact / `/our-services` / `/workshops` address is canonical; the privacy policy's "201 E Hobson Ave, Sapulpa, OK 74066" body text is stale and must be corrected when the privacy policy is migrated into Payload. Flag for editor follow-up at import time per `CONTENT_MIGRATION`. | 2026-05-20 |

---

## Phase 1 implementation (P1)

Implementation work inside the active Phase 1 — moved out of `ROADMAP.md` as each item ships so the roadmap stays a short list of what's open. IDs `P1-*`.

**Convention for closing a Phase 1 item:**

1. Delete the bullet from `ROADMAP.md` (do **not** just flip the checkbox — open lines stay open).
2. Add a row to the table below with a fresh `P1-*` ID, a short summary, and the close date.
3. If the item touched shared specs (ARCHITECTURE.md, INTEGRATIONS.md, DESIGN_SYSTEM.md, etc.), reconcile the doc in the same commit so the spec and the code agree.

The roadmap is the punch list. This table is the audit trail.

| ID    | Task                                                                        | Output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Closed     |
| ----- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| P1-1  | gitleaks pre-commit hook + GitHub Actions secret scan job                   | `.husky/pre-commit` runs gitleaks against staged files; `.github/workflows/security.yml` runs the gitleaks CLI on push/PR. Replaced the paid `gitleaks/gitleaks-action@v2` with the direct CLI (commit `fa2eaa5`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-05    |
| P1-2  | Quality CI workflow (typecheck / lint / format:check)                       | `.github/workflows/ci.yml` — typecheck + lint + format:check on push to any branch and PR to main. Pre-commit runs lint-staged. Test pipeline (Vitest + Playwright + axe + Lighthouse) is still a separate open item.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-05    |
| P1-3  | Next.js + Payload + Tailwind scaffold from spike                            | Landed at repo root via the `spike/stack-validation` merge (D-13). Next 16.2.6 + Payload 3.84 + Postgres 16 + Tailwind v3.4 + Lexical, Husky/lint-staged, Playwright + Vitest configs all present in the working tree. Versions pinned in `package.json`. S3 fallback deferred to Task 1.x.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-05-15 |
| P1-4  | Apply D-1 design tokens to Tailwind v3 config + `:root` CSS vars            | DESIGN_SYSTEM.md §14 translated into `tailwind.config.mjs` (ramps + state colors as hex; semantic colors via `var(--color-…)`; type scale tuples with bundled line-height + tracking; radius / shadow / motion / z-index / containers) and `:root` custom properties in `src/app/(frontend)/styles.css` (the swap layer).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-05-20 |
| P1-5  | Base layout components: SkipToContent + SiteHeader + SiteFooter + MobileNav | Layout in `src/components/layout/` with native `<dialog>` MobileNav (free focus-trap + Escape) and supporting primitives Container / Button / SmartLink in `src/components/ui/`. Placeholder `src/lib/site-content.ts` typed like the future `navigation` + `siteSettings` globals (Phase 2 swap point). Logos at `public/brand/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-05-20 |
| P1-6  | CSP proxy + `/api/csp-report` endpoint + static security headers            | Nonce-based CSP generated per request in `src/proxy.ts` (Next 16 rename of `middleware.ts` — applied alongside via the `middleware-to-proxy` migration). Policy builder in `src/lib/csp.ts`; env-driven mode (`CSP_MODE=enforce \| report-only \| off`, default `report-only`); path-scoped `style-src` for `/admin/*` (Lexical editor). Report sink at `src/app/(payload)/api/csp-report/route.ts` accepts both legacy `application/csp-report` and modern Reporting API payloads. Static security headers wired via `next.config.ts` headers(). Reconciled ARCHITECTURE.md §6, INTEGRATIONS.md §8, ERROR_PAGES.md, LOCAL_DEVELOPMENT.md to use "proxy" terminology.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-05-20 |
| P1-7  | `/api/health` ALB target-group probe                                        | `src/app/(payload)/api/health/route.ts` returns 200 `{ status, uptime, db, timestamp, responseTimeMs }` after a cheap `payload.count({ collection: 'users' })` round-trip; 503 when the DB ping throws. `cache-control: no-store` so CloudFront / ALB never cache the result. Roadmap bullet covered both this endpoint and the CloudWatch alarms that watch it; the alarms half stays open under the CDK bullet (ARCHITECTURE.md §8 alarms table is its spec).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-05-20 |
| P1-8  | HubSpot + GTM nonce-aware bootstrap                                         | Three new server components in `src/components/integrations/`. `ConsentDefault.tsx` ships the inline GTM consent default (all `denied` + `functionality_storage: 'granted'`, `wait_for_update: 500`) and the `__hs_opt_in_consent` bridge listener as a plain nonced `<script>` (next/script's `beforeInteractive` queue trips a React 19 dev-mode nonce hydration warning; `suppressHydrationWarning` covers the documented React 19 nonce-strip behavior). `GtmScript.tsx` + `HubSpotTracking.tsx` use `next/script strategy="afterInteractive"` and are env-gated on `NEXT_PUBLIC_GTM_ID` / `NEXT_PUBLIC_HUBSPOT_PORTAL_ID` so unset local / CI environments don't hit third-party endpoints. ESLint rule `@next/next/no-before-interactive-script-outside-document` disabled project-wide (Pages-Router-era; App Router places these in root layout).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-05-20 |
| P1-9  | Test pipeline workflow: Vitest + Playwright + axe + Lighthouse CI           | New `tests` job in `.github/workflows/ci.yml` runs after `quality`: spins up Postgres 16 as a service container, installs Playwright Chromium + system deps, then `vitest` → `next build` → `playwright test` → `lhci autorun`. Added `@axe-core/playwright` and `@lhci/cli` dev deps; `tests/e2e/a11y.e2e.spec.ts` asserts zero WCAG 2.2 AA violations on `/`. `.lighthouserc.cjs` enforces §7 budgets for accessibility / best-practices / SEO at ≥ 0.95 today; Performance / LCP / TBT / CLS are staged as warnings until Phase 5 "Polish" tunes the real archetype pages to the launch targets. `vitest.setup.ts` updated to load `.env.local` before `.env` (Next-style precedence). CI artifacts: playwright-report/ + .lighthouseci/ uploaded with 7-day retention. Postgres-on-port-5433 convention matches local docker-compose. 20/20 E2E + 1/1 integration tests green locally; CI run validates end-to-end on next push.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-05-21 |
| D-14  | Google Workspace SSO for `/admin` (was open ROADMAP D-14)                   | Spec 001 shipped (`specs/001-google-oauth-sso/`). Custom OAuth integration in `src/app/(payload)/api/auth/oauth/{authorization,callback}/google/route.ts` plus `src/lib/auth/{google-oauth,session-cookie,sign-in-audit,enforce-domain,apply-bootstrap-role}.ts`. ID-token verification via `jose` against Google's JWKS. Users collection: `auth.disableLocalStrategy: true`, fields `email`, `name`, `roles`, `googleSub` (unique). BeforeChange hook chain enforces domain allowlist + bootstrap-admin role + role-update guard. Custom CTA + error UI injected via `admin.components.beforeLogin` (Payload's default LoginView auto-hides `<LoginForm />` when `disableLocalStrategy` is on). `payload-auth-plugin` was originally chosen in ADR 0002 but dropped during /speckit-implement (305-star single-maintainer trust surface, exact-pinned vulnerable deps); ADR 0002 listed the custom-strategy fallback. Vitest int tests cover the application-side hooks + `googleSub` uniqueness; Playwright covers the `LoginError` contract. SES dependency for admin auth dropped (SC-007). `npm audit --omit=dev --audit-level=high` added to CI `quality` job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 2026-05-24 |
| P1-10 | AWS CDK 5-stack provisioning (spec 002)                                     | New top-level `infra/` directory with CDK 2.257 + TypeScript. Five stacks per env (`SeqtekStaging*` / `SeqtekProd*`): Network (VPC, 4 SGs, OIDC + deploy roles), Data (RDS Postgres + S3 media bucket + Parameter Store), Compute (ECR + ALB + ASG + LaunchTemplate), Edge (CloudFront + ACM + Route 53), Observability (SNS + Slack Lambda + 9 alarms + heartbeat). Per-env config block in `infra/cdk.json`. Staging deployed end-to-end on `https://seqtek-preview.com` (Route 53-registered preview domain). 94 / 94 vitest assertions green (network 11, IAM invariants 8, data 12, compute 14, edge 13, observability 19, slack-lambda 17). `infra/test/iam-invariants.test.ts` enforces least-privilege: any policy with a broad-action wildcard on `Resource: ['*']` fails the build (allowlist for the documented `ecr:GetAuthorizationToken` / `cloudwatch:PutMetricData` / `ssm:DescribeParameters` / OIDC custom-resource exceptions). **Validation-period deviations documented in code and flagged for Phase 5.5 launch-readiness review**: ASG runs in public subnets (no NAT yet), Lambda runs without VPC config, RDS single-AZ. Cross-stack reference cycles broken via `EdgeStack` importing the media bucket by attributes and managing its policy manually.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2026-05-28 |
| P1-11 | Productionized Dockerfile + `.dockerignore` + shared ECR repo               | Dockerfile from D-13 productionized for ECR push: removed `yarn.lock` / `pnpm-lock.yaml` branches (npm-only), added `HEALTHCHECK CMD wget --quiet --tries=1 --spider http://localhost:3000/api/health`, added `tini` as PID 1 for clean SIGTERM handling on rolling refresh, copied `public/` into the runtime image (Next.js standalone build doesn't bundle it), non-root `nextjs` user retained. `.dockerignore` exclusion list covers source-control + tooling (`.git`, `.github`, `.husky`, `.specify`, editors), build outputs (`node_modules`, `.next`, `out`, `build`, `dist`, `.turbo`, `coverage`), env files (`.env*`), tests, repo docs, `infra/`, log files, alternative package managers — Constitution IV satisfied: no `.env*` ever reaches the image. ECR repository `seqtek-website` provisioned by `ComputeStack` with lifecycle "keep 10 tagged + 7d untagged"; created in staging and imported by name in prod via `Repository.fromRepositoryName()` (shared image registry across envs).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-05-28 |
| P1-12 | GitHub Actions CI/CD (cdk diff + blue-green deploy via OIDC)                | Three workflows under `.github/workflows/`: `ci.yml` extended with `lint-workflows` (actionlint via the upstream installer) and `infra-synth` (cdk synth both envs + vitest) jobs; `infra-diff.yml` posts a sticky `cdk diff` comment on PRs touching `infra/**` / `Dockerfile` / deploy workflows (`<!-- speckit-infra-diff -->` marker; updates in place on subsequent pushes; staging OIDC role for read-only diff); `deploy.yml` triggers on push-to-main and `workflow_dispatch` with concurrency `deploy-staging:cancel-in-progress:false`, builds via `docker/build-push-action@v6` with `cache-from/to: type=gha`, pushes `:<sha7>` + `:latest`, runs `cdk deploy SeqtekStaging*Compute`, explicitly calls `start-instance-refresh` (LaunchTemplate doesn't change when only the upstream image content changes — without the explicit trigger the ASG silently keeps the old image), polls `describe-instance-refreshes` up to 15 min, then the smoke job runs in `mcr.microsoft.com/playwright:v1.58.2-jammy` against the live `SeqtekStagingEdge.SiteUrl`. GitHub OIDC provider + per-env deploy roles in `infra/lib/deploy-role.ts`: prod sub-claim pinned to `repo:SeqTechLLC/seqtekdotcom:ref:refs/heads/main`, staging accepts any branch; policies grant `sts:AssumeRole` on `cdk-*` toolkit roles, CloudFormation on env-scoped stack ARN, ECR push/pull on the shared repo, ASG `Start/CancelInstanceRefresh` scoped to the env prefix + `Describe*` on `*` (autoscaling resource-level permissions don't apply to Describe actions). Out-of-band: `AWS_ACCOUNT_ID` repo variable, `staging` GitHub Environment, branch protection on `main` with 6 required checks (`cdk diff` deliberately excluded — the workflow's `paths:` filter would block non-infra PRs). **SC-002 measured at 7m45s** on the first real round-trip merge — well inside the 20-min budget. **Production CD auto-deploy deferred** until leadership approves the `seqtek.com` DNS cutover; `deploy-prod.yml` and `production` GitHub Environment land at Phase 6. | 2026-05-28 |
| P1-13 | CI path-gating: skip heavy jobs on docs/infra/workflow-only changes         | Post-Phase-1 CI tuning (PR #33). Added a `changes` job (`dorny/paths-filter@v4`) to `.github/workflows/ci.yml` and gated the expensive jobs on its outputs: `tests` (Playwright + axe + Lighthouse) and `int-tests` (Vitest) run only when `src` changed, `infra-synth` (CDK synth + assertions) only when `infra/**` changed, `lint-workflows` (actionlint) only when `.github/workflows/**` changed; `quality` (tsc/lint/format) and `secrets` (gitleaks) stay always-on (`format:check` covers `*.md`/yaml; gitleaks is a security gate). The `on:` trigger stays broad (no top-level `paths:`) so the workflow always runs and every required job emits a check run — a job skipped by `if:` reports `skipped`, which branch protection treats as a satisfied required check, leaving the six required contexts on `main` unchanged (a _workflow-level_ `paths:` filter would instead leave them Pending and block merges — the same trap that kept `cdk diff` out of required checks in P1-12). Verified on the PR itself (a workflow-only change): the three gated required checks reported SKIPPED while the PR stayed MERGEABLE/CLEAN; wall-clock fell from ~7m47s (PR #32, a 13-line docs diff that paid the full Playwright/Lighthouse run) to ~1m26s. `src` is an allowlist — safe failure direction: an unclassified path skips the heavy tests rather than false-passing, so new source-relevant root configs must be added to the filter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-06-07 |

---

## Phase 2 implementation (P2)

Spec 003 (content models). Same convention as P1: items move out of `ROADMAP.md` once shipped and land here as `P2-*` rows.

| ID   | Task                                                                     | Output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Closed     |
| ---- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| P2-1 | Phase 2 foundational + US1 MVP block library framework (PR #11)          | 13 content collections (`pages`, `posts`, `caseStudies`, `services`, `servicePillars`, `workshops`, `industries`, `locations`, `teamMembers`, `testimonials`, `categories`, `media`, `users`) + 3 globals (`siteSettings`, `homepage`, `navigation`) wired through `src/payload.config.ts`. Lexical editor config + the inline block set (`Callout`, `Disclosure`, `Figure`, `ImageWithCaption`, `InlineCta`, `QuotePullquote`, `TestimonialEmbed`). The layout block library in `src/payload/blocks/layout/` with matching React renderers in `src/components/sections/` dispatched via `RenderBlocks.tsx` + `registry.ts`. Conditional `required` helper + slug-from-title hook + revalidate-on-change hook. Storage adapter (conditional S3 plugin wiring deferred to US6). Live preview URL builder + `livePreviewFor` factory. Initial migration `20260526_225009` (later collapsed in P2-6). Editor authors via real block UI; public render verified against Lexical→JSX converter.                                                                                         | 2026-05-30 |
| P2-2 | Phase 4 US2 — live preview + close Phase 3 stragglers (PR #13)           | Live-preview URL builder and `livePreviewFor` factory wired into Pages/Posts/CaseStudies/Services. `/preview/[collection]/[slug]` route handler is same-origin (admin session cookie); the earlier URL-secret design was rejected and the contract updated (`contracts/live-preview-urls.md`). `<PreviewBanner />` at `src/components/layout/PreviewBanner.tsx` wired into the `/showcase/[slug]` dev route. T074–T077 cover redirect mechanics per collection (302 + Location + `__prerender_bypass` cookie). T079 staging revalidation E2E lands as a `test.skip` skeleton — public page templates (spec 004) are the prerequisite.                                                                                                                                                                                                                                                                                                                                                                                                                                              | 2026-05-30 |
| P2-3 | Phase 5 US3 — render-path ergonomics (PR #14)                            | Type-level non-nullable assertions on `Page` / `CaseStudy` / `Post` / `Service` / `HeroBlock` in `tests/int/render/typesNonNullable.int.spec.ts` (13 cases). Public draft-query invariant in `tests/int/render/publicDraftQuery.int.spec.ts` — `caseStudies` draft hidden from anon `payload.find`, pages published doc surfaced. `LOCAL_DEVELOPMENT.md` "Common Tasks" gains "Regenerating Payload types" + "Regenerating the Payload importMap" subsections; `PAYLOAD_DEVELOPMENT.md` §7 (inline blocks) + §14 (custom components) link to the importMap workflow. 76/76 render-path int tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 2026-05-30 |
| P2-4 | Phase 6 US4 — Wix audit seed pipeline (PR #15)                           | Audit seed pipeline at `src/payload/seed/migrateFromAudit.ts` — idempotent `upsertBySlug` via Payload Local API, slug-rewrite map (INTEGRATIONS.md §9), plain-text-to-Lexical transformer per CONTENT_MIGRATION.md §4, five per-source parsers (caseStudies / pages / posts / homepage / siteSettings), and an R-16 `migration-errors.log` writer. CLI supports `--dry-run`, `--collection=<name>`, `--recrawl-images` (wired, post-Phase-2 execution per R-10), `--help`; env validation maps to the exit-code table in `contracts/seed-cli.md`. 7/7 seed int tests pass. **Frozen at this shape per the marquee-pivot decision (2026-05-31): the pipeline ships as a one-shot migration tool + 301-redirect-map source, not a publish baseline. No further investment in seed-side polish; subsequent content lands via the admin UI on top of the marquee-page foundation.**                                                                                                                                                                                                    | 2026-05-30 |
| P2-5 | Phase 7 US5 — access matrix + draft-leak invariants (PR #16)             | Access matrix locked behind a data-driven int test at `tests/int/collections/access.int.spec.ts`, plus T112's FR-017 regression guard (no local-strategy backdoor on `users`) co-located. Draft-leak coverage at `tests/int/collections/draftLeak.int.spec.ts` (35/35) verifies `NotFound`/`Forbidden` distinction per FR-016 and parity between REST list / detail / GraphQL surfaces. Two new Playwright specs: `firstSignInBootstrap.e2e.spec.ts` (cookie-mint smoke always-on; bootstrap-table-wipe gated behind `E2E_ALLOW_DESTRUCTIVE=true` / `CI`) and `editorDeleteForbidden.e2e.spec.ts` (UI affordance: `#action-delete` absent on editor session). 358/358 int suite green. Pre-PR refactor: `tests/helpers/` split into `tests/seeders/` / `tests/fixtures/` / `tests/sessions/` per the long-pending convention. ARCHITECTURE.md §6 access table updated with per-collection overrides (`categories` admin-only mutations, `testimonials` `isActive` gating, `users` create-always-denied + admin-only update/delete).                                                | 2026-05-30 |
| P2-6 | DB migration collapse + Postgres 16 → 18 upgrade (PR #17)                | Deleted the spike-era `20260526_225009` migration (Phase 1 D-13 era) and regenerated a single `20260531_141253_init` covering the full Phase 2 schema (3622 TS / 28k JSON snapshot). Root cause: PRs #11/#13/#14/#15 added collections/fields without generating follow-up migrations, so staging's container-side `payload migrate` ran cleanly but applied nothing — the homepage 500 on post-deploy smoke (#13, #14, #15) was the symptom. Pre-launch is the cheap window to collapse. Bundled the PG 16 → 18 major-engine bump in the same staging-wipe window (4 pinned places: CDK `VER_18_3` + `allowMajorVersionUpgrade: true`, `docker-compose.yml` `postgres:18` with mount moved to `/var/lib/postgresql` per PG18's version-namespaced subdir convention, `vitest.globalSetup.ts` testcontainers, `.github/workflows/ci.yml` service container). 358/358 int tests green against testcontainers PG18. Staging runbook documented in PR body; SSM port-forward → psql wipe → ASG instance refresh → second wipe (race fix). Smoke green 2026-05-31, staging on PG 18.3. | 2026-05-31 |
| P2-7 | Phase 2 close — US6 media (S3) + US7 scheduled-publish + Polish (PR #19) | US6: conditional `@payloadcms/storage-s3` registration in `payload.config.ts` (only when `S3_BUCKET` set), alt-required server-side validation, MIME + 25 MB size cap, staging upload smoke. US7: `enforceDraftWhenScheduled` wire-up audit + integration test across every draftable collection with `publishedAt` (the Payload-side invariant; cron trigger intentionally still deferred). Polish: Vercel react-best-practices audit follow-ups. Closes the spec-003 carry-over; Phase 2 fully shipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-06-01 |

---

## Phase 3 implementation (P3)

Spec 004 (public render foundation + marquee page templates). Same convention as P1/P2: items move out of `ROADMAP.md` once shipped and land here as `P3-*` rows. Spec 004's scope was **engineering only** — templates that render against Payload data; published marquee _content_ is content-lead-gated and tracked in `ROADMAP.md` (§1 C-\* items + the deferred content track), not here. **P3-3** captures the later, post-spec-004 render/UI/IA polish on those same templates (image rendering, the warm-paper visual system, the `/workshops` IA fix) — emergent work, no new spec.

| ID   | Task                                                                                            | Output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Closed     |
| ---- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| P3-1 | Public render foundation + 5 marquee page templates (PR #21)                                    | All 47 spec-004 tasks (T001–T047). **Cached-reader foundation** in `src/lib/payload.ts`: layered `cache(unstable_cache(...))` chrome + detail + list readers, published-only (`overrideAccess: false`, `draft: false` — no draft leak), `revalidate: 3600` + on-demand tag invalidation. The load-bearing **tag-parity invariant** (reader `tags` === `buildRevalidatePlan(...)` tags) is pinned by the keystone test `payload-cache-tags.int.spec.ts`. **ISR strategy reconciled in ADR 0005** — routes render dynamically (per-request CSP nonce via `headers()`) while page DATA flows through `unstable_cache`, so on-publish-instant + 1h fallback holds without `generateStaticParams`. **Marquee templates:** homepage (`homepage` global → bespoke sections, drift #2 retired), `/case-studies/[slug]` + listing, `/team`, `/touchstone-workshops/[slug]` + listing, generic `/[slug]` for `pages` (localshoring; retired the `/showcase` + `/about` spike routes, drift #3). **Remaining in-scope routes:** `/insights` + detail (Article JSON-LD), `/services` overview + `/services/[pillar]` + nested `/services/[pillar]/[slug]` (drift #1 — nested revalidation paths). **SEO baseline:** `src/lib/metadata.ts` + `src/lib/structured-data.ts` (Organization / Article / BreadcrumbList JSON-LD, CSP-nonce-safe, `</script>`-breakout-hardened) + dynamic `sitemap.ts` (degrades to static paths on empty DB). **Error/maintenance:** 404 / 500 (`error.tsx` + `global-error.tsx`, visible request id) / maintenance 503 via `proxy.ts` (`/api/health` exempt so the ALB doesn't cycle instances) + `x-request-id` correlation. **301 redirect map** in `next.config.ts` from INTEGRATIONS §9. WCAG-AA contrast fixes on shipped marquee surfaces (`bg-accent` → `accent-strong`). 443/443 int tests green; ADR 0005 added; ARCHITECTURE §3 ISR table + ROADMAP/spec docs reconciled in-PR. Acceptance = template-scope per the 2026-06-01 clarifications.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 2026-06-01 |
| P3-2 | PR #21 review follow-ups — 404 dead link + pillar-move revalidation (PR #23)                    | Two non-blocking PR #21 review items. **404 dead link:** `not-found.tsx`'s primary CTA ("Book a strategy call") pointed at the unbuilt `/contact`, so the most prominent 404 action was itself a 404 — repointed to `mailto:contact@seqtek.com` (canonical contact email, mirroring the error-page fallback), then to the real `/contact` route once spec 005 shipped it (#25, `P5-3`). **Pillar-move revalidation:** `revalidateOnChange.ts` busted only the new pillar's paths when a service moved pillars (A→B), leaving stale `/services/A/<slug>` + `/services/A` cached until the 3600s fallback; now resolves `previousDoc`'s pillar too (only when the pillar actually changed, so the common save stays one fetch) and emits both pillars' nested + landing paths. Slug-based cache tags are unchanged, so the keystone tag-parity invariant (C1) still holds. Unit case pins the pillar-move path set; 444/444 int tests green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 2026-06-02 |
| P3-3 | Render/UI/IA polish on the spec-004 templates + warm-paper visual system (PRs #47–#51, #54–#60) | Post-spec-004 polish that completes the marquee templates as real content/photos landed (rendered against the gitignored local content mirror + seeders; published marquee content stays content-lead-gated, unchanged). **Image rendering wired onto text-only templates:** case-study `heroImage` (#57), insight `featuredImage` (#59), and mid-post insight illustrations via a new `tools/leonardo-images/` pipeline (Leonardo Phoenix → figure-block splice over the Local API + a REST staging mirror; output gitignored) with an `max-w-none` body fix so title/image/body share one column edge (#60); workshop `deliverables[]` rendered as a "What you leave with" checklist + `BrandTeaser` single-column fallback when no image is set, plus `PayloadRestClient.updateGlobal()` for the homepage-global seeder (#56); `TeamGrid` "cards" layout → full-bleed `aspect-[4/3]` photos served through `ResponsiveImage` srcset instead of a 96px avatar circle (#47); listing cards (`PostList`/`CaseStudyGrid`) made whole-card-clickable with `aspect-[16/9]` crisp images (#58). **Warm-paper visual system (#55):** the composed folksy-not-plain design direction as a global system — body on warm paper (`#faf8f4`), one shared `container-lg` grid rail across every section + header + footer + listing, one bordered-white-card grammar, values as a rule-separated two-column editorial index, display-scale hero; container padding moved outside the max-width box so header/footer stop offsetting 32px from section content; AA contrast follow-up (`text-muted` `#6e6e7a`→`#65656f`, `surface-subtle` retuned so the brand-green eyebrow still clears AA on the band); `JsonLd` `suppressHydrationWarning` on the browser-stripped nonce attr. **Section-grid alignment (#54):** `VideoEmbed` → `container-lg` card with anchored caption + optional eyebrow field ("From the SEQTEK Podcast"); `StatsBar` columns follow item count; migration adds the `eyebrow` column. **Workshop proof section (#50):** `Workshops` gains `photos[]` (upload + caption) + a `video` group; detail template renders a 2-up gallery + `VideoEmbed`; CSP `frame-src` allowlists `youtube-nocookie` + `vimeo`; migration `20260611_201340`. **IA fix (#49):** `/touchstone-workshops` → `/workshops` — there is ONE Touchstone workshop among three (the branded umbrella made Five Dysfunctions / Re-Alignment read as Touchstone sub-types, and the listing's `/workshops/<slug>` hrefs 404'd against the unbuilt route); route dir renamed, de-branded listing, 301 map + breadcrumbs/sitemap/nav/homepage-CTA/revalidate-path updates, INTEGRATIONS §9 + CONTENT-REQUIREMENTS reconciled. **YouTube error-153 fix (#51):** `referrerPolicy` `no-referrer` → `strict-origin-when-cross-origin` across `VideoEmbed`/`Hero`/`Map`/`Embed` (YouTube now requires the `Referer` to validate the embedding origin); a11y suite excludes third-party player frames from axe. **Quill favicon (#48):** Q-and-quill mark from the brand wordmark — `favicon.ico` 16/32/48 + `app/icon.png` 512. 508/508 int + a11y e2e green across the batch. | 2026-06-14 |

---

## Phase 5 implementation (P5)

The code-owned subset of Phase 5 "Polish": specs 005 (HubSpot Forms engine + live workshop form), 006 (consent bridge + privacy surfaces + CSP enforce machinery), 007 (launch hardening & a11y/perf), 008 (GTM pixel activation, code/doc track), 009 (media via CloudFront `/media/*` + serverURL, ADR 0008), 010 (block-composed pages — two content primitives, ADR 0009), plus the case-study importer tool and a non-spec launch-hardening deploy/dependency batch (P5-7). Same convention: shipped items move out of `ROADMAP.md` and land here. IDs run by archival order, not strictly by date — 007/008 were reconciled first (P5-1/P5-2); 005/006/importer were backfilled later as P5-3/P5-4/P5-5 (PR #28); 009 closed as P5-6 (PRs #43/#44); P5-7 (PRs #46/#52) is a later non-spec hardening batch; 010 closed as P5-8 (PR #66). Residual launch-readiness gates (formal SR sign-off, the perf warn→error flip, CSP enforce, cross-browser QA, structured-data/OG) plus spec 008's external GTM-UI/staging config tail + named deferrals stay in `ROADMAP.md` under Phase 5 / 5.5.

| ID   | Task                                                                                  | Output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Closed     |
| ---- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| P5-1 | Launch hardening: accent contrast + a11y audit + slow-request + perf proof (spec 007) | **US1 — accent contrast (MVP):** per-usage remediation of ~40 meaning-bearing brand-green-500 accents → green-700 `accent-strong` family across `src/components/sections/*`, `richText/inline/*`, and the case-study/workshop/not-found routes (foreground text, solid CTA fills, blockquote/metric rules, hover borders); navy-surface accents (`HomepageHero`/`StatsBar`, ~6.95:1) and the decorative `Content` `bg-accent/5` wash left as-is. The load-bearing finding: the `text-accent` utility resolves to green-500 (decorative seed), not the green-700 text token — documented in DESIGN_SYSTEM §2.4/§2.2 so it can't recur. Inventory at `specs/007-launch-hardening-polish/accent-audit.md`. **US2 — a11y audit:** `tests/e2e/a11y.e2e.spec.ts` expanded from homepage-only to the full 14-route in-scope set (seeded via the reusable `tests/e2e/helpers/seedInScopeRoutes.ts`), zero WCAG 2.2 A/AA axe violations + landmark/heading-order/keyboard-focus/alt/reduced-motion assertions; fixed the listing/grid `h1→h3` heading skip via a `headingLevel` prop on the six grid components; SR test script + best-effort pass at `sr-pass.md`. **US3 — slow/hung reads:** `withReadTimeout` (`Promise.race`, 5s) as the outermost layer of the 16 cached readers in `src/lib/payload.ts`, throwing `PayloadReadTimeoutError` → branded `error.tsx` + a warn log carrying `x-request-id` read via `headers()` outside `unstable_cache`; `/api/health` exempt by construction (ADR 0007; `tests/e2e/slow-request.e2e.spec.ts` + `tests/int/lib/read-timeout.int.spec.ts`). **US4 — perf proof:** §7 proven once in a seeded production build and recorded (`perf-results.md`) — a11y 1.00 / SEO 1.00 / CLS 0.000 on every public route, Performance 0.95–0.96 on detail routes; gate left at `warn` (flip → Phase 5.5). 49/49 a11y+US3 E2E green; DESIGN_SYSTEM §2.2/§2.4, ERROR_PAGES §5, ARCHITECTURE §7 reconciled; ADR 0007 added.                                                                                                                                                                                                                                                                                                                                                                                                                                | 2026-06-05 |
| P5-2 | GTM pixel activation — code/doc track (spec 008, PR #31)                              | The in-repo half of spec 008; the external GTM-UI/staging config tail + named deferrals stay open in `ROADMAP.md`. **US3 — conversion-signal surface:** one SSR-safe `pushDataLayer` emitter (`src/lib/analytics/dataLayer.ts`) with a typed `AnalyticsEvent` union as the **single** `window.dataLayer` writer (INV-1/2/4 enforced by structure, not comments). `cta_click` via a new `TrackedCtaLink` primitive that every CTA surface routes through (header/mobile nav, `ContactCta`/`CtaSection`/`InlineCta`, `Button`) — push wrapped in try/catch so analytics can never swallow a navigation, `onClick` forwarded so the mobile drawer still closes; `case_study_view` via a fire-once, StrictMode-safe `TrackView` island on `/case-studies/[slug]` (`useRef` guard survives React dev double-invoke); `booking_complete` as a dormant `BookingCompleteSeam` `message` listener (cannot fire today — no sender; `origin` gate flagged in its go-live NOTE for when the live HubSpot Meetings embed replaces the `HubspotMeetings` placeholder). `hubspot/submit.ts` refactored onto the shared emitter, dropping its own `dataLayer` global — `form_submission_*` shapes unchanged (INV-3, `hubspot-submit` int suite stays green). **US4 — CAPI consent decision:** recorded in INTEGRATIONS.md §2.3 — the site originates no CAPI events, so there is no in-app enforcement obligation; if CAPI continues post-`seqtek.com`-cutover, enforcement is at the off-site source (owners Megan/Domanick). **Docs:** INTEGRATIONS.md §2.4 event table corrected (the stale fictional `form_submission`/`contact_form_submit` rows replaced with the real `form_submission_attempt/success/failure` + the three new events, each with payload/trigger/Live-or-Seam status); container ID `GTM-54KBJ2Z3` + the GTM-UI build/staging/export tail captured in `infra/gtm/README.md`; constitution v1.2.0 added the external-verification carve-out that lets US1/US2/US4 be verified outside the repo. Tests authored first: `tests/int/lib/dataLayer.int.spec.ts` (all three event shapes + absent-`window`/absent-`dataLayer` no-op paths) and `tests/e2e/datalayer-events.e2e.spec.ts` (self-seeds a published case study via the Local API, asserts the live pushes incl. exactly-one `case_study_view`). 10/10 dataLayer+hubspot-submit int green; typecheck + lint clean. | 2026-06-06 |
| P5-3 | Spec 005 — HubSpot Forms API engine + live workshop form (PRs #24, #25)               | Custom React forms submitting to the HubSpot Forms API per INTEGRATIONS.md §1.2, sharing one engine. **Engine** (`src/lib/hubspot/submit.ts`): `idle→submitting→success\|error` state machine; 4xx no-retry / 5xx·network·timeout single-retry + 1s backoff / 15s timeout (~31s worst case, accepted per §1.2); `form_submission_{attempt,success,failure}` dataLayer events; `hutk` + page context; `legalConsentOptions` passthrough. Native HTML validation — **no Zod dep** (the spec-004 deferred item assumed Zod; dropped deliberately to avoid a runtime dep). **Half-wired seam** (mirrors the spec-005 GUID seam): the live POST fires only when portal ID + a valid form GUID are set, otherwise a stub success exercises the full client lifecycle. **Workshop Inquiry Form** provisioned (region na1, no CAPTCHA, no consent; confirmed 2026-06-02) and live on the workshop route. **Contact:** `ContactForm` + new `/contact` route, half-wired pending its own GUID (→ ROADMAP gated item). Honeypot spam guard (hidden from humans + the a11y tree → silent success without POST, since CAPTCHA is off on the API forms). `api.hsforms.com` added to CSP `connect-src`. Newsletter dropped (no program; old site had none) — `newsletter-cta` block left dormant. gitleaks allowlist for the public Workshop GUID. Tests: `hubspot-submit.int.spec.ts` (5) + `hubspot-forms.e2e.spec.ts` (lifecycle, validation, dataLayer, honeypot hidden, axe-clean). §1.2 form-provisioning checklist added in #24.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 2026-06-02 |
| P5-4 | Spec 006 — consent bridge + privacy surfaces + CSP enforce machinery (PR #26)         | The consent half of spec 005. **Keystone fix:** `ConsentDefault.tsx` now drives the HubSpot→GTM bridge off the official `_hsp.push(['addPrivacyConsentListener', cb])` API instead of the unofficial `__hs_opt_in_consent` DOM event (which appears nowhere in HubSpot docs and most likely never fired, pinning consent at all-denied). The callback maps `consent.allowed \|\| consent.categories.{analytics,advertisement}` to Consent Mode v2 signals and fires `hubspotConsentUpdate`. INTEGRATIONS.md §2.2 reconciled in the same change; recorded in **ADR 0006** (index backfilled for 0004/0005). **New surfaces:** `ConsentPreferences` footer control (`showBanner` / `revokeCookieConsent`, inert when HubSpot absent) mounted in `SiteFooter` legal nav (US4); `/privacy-policy` static route with disclosures, canonical Cheyenne address, sitemap entry, LHCI coverage (US5 — legal prose a flagged draft pending Phase 5.5 sign-off); `docs/CSP_VIOLATIONS_KNOWN.md` triage catalogue (US2); `infra/gtm/README.md` container export/commit workflow. **CSP:** machinery + soak gate in place; code default stays **report-only** (Constitution IV) — the enforce flip is the gated env change (T014/T015), `style-src 'self'` vs HubSpot-banner risk assigned to the soak. ARCHITECTURE.md §6 CSP table reconciled to `lib/csp.ts`. 462 int + new e2e green (`csp.int`, `consent-flows.e2e` US1/US3, `privacy-consent-ui.e2e` US4/US5 axe-clean; reusable `tests/e2e/helpers/consent.ts` harness; `integrations.e2e` moved off the dead event). **Gated tail (NOT shipped, stays in ROADMAP):** GTM container + Wix pixel IDs + live fire-matrix (T007–T009), ≥7-day soak → prod Parameter Store enforce cutover (T014–T016), live returning-visitor + cross-browser QA (T019/T030), CloudWatch per-directive alarm in the infra/CDK repo (T013).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-06-04 |
| P5-5 | Case-study REST importer tool (PR #27)                                                | `tools/import-case-study/` — create or update a single case study from a self-contained JSON file over the Payload REST API, idempotently by slug, without the admin UI. SSO session-JWT auth (`Authorization: JWT <token>` — no API key, no auth-schema change); uploads hero/logo/ogImage to the `media` collection first, resolves industry/services/related relations by slug, converts prose to Lexical via the seed pipeline's `htmlToLexical`. Draft by default (`--publish`, `--dry-run`); mirrors the media 25 MB / MIME guards client-side. 10 unit tests with an injected `fetch` (no server or DB). **Known follow-up → ROADMAP T-1:** re-uploads the hero on every re-import, orphaning the previous `media` row; needs hash-based reuse like the audit pipeline (`CONTENT_MIGRATION.md` §8) + optional old-hero pruning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2026-06-05 |

| P5-6 | Spec 009 — media via CloudFront `/media/*` + serverURL (ADR 0008; PRs #43, #44) | The fix for the first staging content seed's broken media (10/12 images on `/team`, `http://localhost:3100/api/media/file/…` URLs). Three root causes closed (ADR 0008 Proposed → **Accepted**): **(1) serverURL** — `next_public_site_url` SSM param provisioned by DataStack whenever `cfg.domainName` is set (prod inherits at Phase 6 by config); the user-data basename\|uppercase loop maps it with zero compute changes; also fixed canonical/OG/JSON-LD/sitemap absolute URLs. **(2) edge serving** — storage adapter sets `prefix: 'media'` + `generateFileURL` → pure `mediaFileURL()` emitting `${NEXT_PUBLIC_SITE_URL}/media/<filename>`; key shape settled to **`media/<filename>`** (clarify 2026-06-09: the `/media/*` behavior has no originPath, so URL path == S3 key verbatim — zero edge changes; neither ADR option mapped without extra config). Hook-order proof from the installed plugin source: core's url afterRead runs first (`mergeBaseFields` deepmerge concat), the plugin's `generateFileURL` hook runs last and wins unconditionally → URLs recomputed on every read, nothing persisted goes stale; local dev/CI untouched (disabled plugin attaches no hooks). **(3) re-key** — `tools/ingest-photos/rekey-staging.ts` moved the 27 seeded objects + 206 variants **in place** (S3 move → per-doc `prefix` PATCH, in that order so the static-handler fallback never broke; `moved=233 patched=27 errors=0`, idempotent re-run clean) — amended at plan time from delete+re-push, which would have minted new IDs and orphaned the hand-wired team-member relations. **FR-011** (stable keys forfeit new-key cache busting): Media `afterChange`/`afterDelete` hooks invalidate the `/media/*` paths (original + variants) via `invalidateCloudFrontPaths`; injectable invalidator because the int suite's `isolate: false` shared module cache defeats `vi.mock`. Staging verification then exposed that `CLOUDFRONT_DISTRIBUTION_ID` + the `cloudfront:CreateInvalidation` grant had **never been provisioned** — the R-03 page-publish invalidation path had silently skipped since launch (zero invalidations ever, masked by 60–120s page TTLs). **PR #44** fixed it: Edge-owned SSM param + distribution-scoped IAM policy attached cross-stack to the newly exposed `appInstanceRole` (same cycle-breaking pattern as the media bucket policy); proven end-to-end — upload → `Hit from cloudfront` → delete → invalidation `I827U20M4WKUVPPNFGMRHU5O4W` (the distribution's first ever) with exactly the original+variant paths → edge 403. Staging evidence on both PRs: `/team` 12/12, zero `localhost` across pages/OG/sitemap, bucket 100% `media/`-keyed, fresh upload renders with no fix-up. Tests: `tests/int/media-url.int.spec.ts` (13, written first) + CDK assertions incl. the domainless-omission and IAM Resource scoping; 505/505 int + 98/98 infra. Docs reconciled in-change: ARCHITECTURE §5/§6 (key shape, cache-busting → invalidation hook, 403→404 re-marked deferred, env-var table), STAGING*INGEST runbook, ROADMAP Phase 6 Edge-deploy→refresh ordering. **Residual:** none in-repo; prod inherits at cutover (checklist line added). | 2026-06-11 |
| P5-7 | Launch-hardening: staging build-args for client IDs + esbuild advisory fix (PRs #46, #52) | Two deploy/dependency fixes that don't belong to a spec. \*\*#46 — `NEXT_PUBLIC*\*`to the staging build:** these values inline into the _client_ bundle at build time, but the CI Docker build passed no args, so the HubSpot portal ID + form GUIDs + GTM ID never reached staging —`isHubspotLive()`saw nothing and every form ran in stub mode despite the values being provisioned (runtime SSM can't fix client bundles — the exact spec-009 research R6 caveat). Dockerfile declares the four ARGs in`builder`(client inlining) and`runner`(server-component reads, not in SSM) with empty defaults so CI stays hermetic;`deploy.yml` passes the public portal ID + workshop GUID. Contact GUID stays empty until the form exists in HubSpot; GTM ID stays empty until the spec-008 container rebuild (`GTM-54KBJ2Z3`is the live Wix-era container and must not load on staging with its legacy tags). Result on the next staging deploy: the Workshop Inquiry form goes live end-to-end; the contact + GTM seams activate by pasting their values into the build-args block. **#52 — esbuild advisories:** GHSA-gv7w-rqvm-qjhr + GHSA-g7r4-m6w7-qqqr (esbuild ≤0.28.0, patched 0.28.1) reach the production tree via`payload → tsx`and`@payloadcms/db-postgres → drizzle-kit`, tripping the CI `npm audit --omit=dev --audit-level=high`gate on every PR; bumped`tsx`4.21.0→4.22.4 + a`$tsx`override (payload exact-pins tsx so it never dedupes to the patched root copy) + a scoped override forcing drizzle-kit's esbuild to`^0.28.1`; scoped not blanket so vite/vitest keep their natural esbuild. Audit exits 0; 506/506 int. | 2026-06-12 |
| P5-8 | Spec 010 — block-composed pages, two content primitives (ADR 0009; PR #66) | Retired every bespoke per-type render template; the site is now two primitives — a block-composed **Page** (`layout`blocks via`RenderBlocks`) and the **Post** (blog richText, untouched). Each specialized type keeps its collection + typed metadata (slug, listing image, SEO, relationships, nested URL) but trades its discrete body fields for a `layout`blocks array spread from the same`layoutBlocks`(so a new block is usable on every type with no per-type code). **Foundation:** two gap-fill blocks`image`+`gallery`(FR-005, the SC-005 coverage audit found images were the only gap → 45 blocks); reading-column enforcement moved fully into the block components (DESIGN_SYSTEM §11.4 / FR-009);`workshops`/`teamMembers`added to live-preview wiring; a field→layout composer scaffold + fidelity/idempotency harness. **Migrations (US1/US2/US5):**`workshops`(pilot/MVP — the acceptance gate),`caseStudies`, `services`, `teamMembers`(gained drafts + a new`/team/[slug]`detail route with Person JSON-LD), and the`homepage` global, each via a Payload expand/contract migration (legacy body columns hidden + read-only, kept one release) + an idempotent, slug-keyed composer (`src/payload/seed/compose/\*ToLayout.ts`; a `runGlobalComposer`for the homepage singleton). **Skills (US3/US6):**`compose-page`(brief →`layout`of existing slugs, or one named gap) and`convert-to-blocks` (reproduce an existing page, or one named gap), each fixture-guarded against the registry; the block-curation loop documented as the one code path (US4, BLOCK_LIBRARY §5.9) with a cross-type reuse guard. **Invariants preserved:** cache-tag parity C1, access matrix (teamMembers → editorial-draftable tier), draft isolation, listings + JSON-LD + nested URLs + sitemap + 301 map; analytics (`cta_click`/`case_study_view`) unaffected (they live in header/route islands, not the section components). Docs reconciled in-change (BLOCK_LIBRARY §1/§5.7/§5.9/§6, ARCHITECTURE §2, DESIGN_SYSTEM §11.4). Visual-verified at desktop + mobile across the migrated routes. **Deferred a release:** `drop_legacy_body_columns`(expand/contract close-out). **Staging rollout is two-step:**`payload migrate` (schema) then run the composers (data) — pages render empty bodies between the two until composed. | 2026-06-16 |

---

## Notes on what is NOT here

- **In-flight items inside an active phase** (e.g., the CDK app, Dockerfile/ECR push, blue-green CI/CD) stay in `ROADMAP.md` until they ship, then move here as a `P1-*` row per the convention above. Completed Phase 1 implementation items leave the roadmap immediately — that's what keeps the roadmap a punch list of open work.
- **ADRs** (`docs/decisions/`) are their own append-only log — they don't need a copy here. Reference them by number when relevant.
